on:
  [push]
name: GEMINI_XS to fp32
jobs:
  dequantize:
    name: Dequantize model
    runs-on: ubuntu-latest
    steps:
      - name: Check out the conversion repository
        uses: actions/checkout@master
      - name: Dequantize and upload the model
        run: |
          docker build -t ci-fp32-dequantize ci/fp32-dequantize/.
          docker run ci-fp32-dequantize
